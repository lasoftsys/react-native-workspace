import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const ScanCard = () => {
  return (
    <View>
      <Text>ScanCard.screen</Text>
    </View>
  );
};

export default ScanCard;

const styles = StyleSheet.create({});
